<%= attachment_image_tag(@user, :profile_image, class: "circle", fallback: 'default_avatar.png') %>
<h1><%= @user.name %></h1>
<ul>
  <li>Wants: <%= @user.want %></li>
</ul>

<% if @user == current_user %>
<%= link_to "Edit Profile", edit_user_path(@user) %>
<% end %>

<!-- <h3>Leave <%= @user.first_name %> an anonymous message:</h3>

<% @comments.each do |comment| %>
  <%= comment.body %>
<% end %>

<%= simple_form_for(Comment.new) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :body %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %> -->
